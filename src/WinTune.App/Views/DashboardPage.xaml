<Page x:Class="WinTune.App.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="32">
        <StackPanel Spacing="24">

            <!-- Health Score -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="12" Padding="24">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel>
                        <TextBlock Text="Estado del PC" Style="{StaticResource TitleTextBlockStyle}"/>
                        <TextBlock x:Name="StatusText"
                                   Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="FindingsText" Margin="0,8,0,0"
                                   Style="{StaticResource BodyTextBlockStyle}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" Spacing="4">
                        <TextBlock x:Name="HealthScoreText"
                                   Text="{x:Bind ViewModel.HealthScore, Mode=OneWay}"
                                   FontSize="48" FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="/ 100" HorizontalAlignment="Center"
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Scan button -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Analizar todo" Style="{StaticResource AccentButtonStyle}"
                        Command="{x:Bind ViewModel.ScanAllCommand}"
                        IsEnabled="{x:Bind ViewModel.IsScanning, Converter={StaticResource NegatedBoolConverter}, Mode=OneWay}"/>
                <Button Content="Cancelar"
                        Command="{x:Bind ViewModel.ScanAllCancelCommand}"
                        Visibility="{x:Bind ViewModel.IsScanning, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                <ProgressRing IsActive="{x:Bind ViewModel.IsScanning, Mode=OneWay}" Width="24" Height="24"/>
            </StackPanel>

            <!-- Stats cards -->
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="16" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Hallazgos" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalFindings, Mode=OneWay}"
                                   FontSize="28" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="16" Margin="4,0">
                    <StackPanel>
                        <TextBlock Text="Espacio a liberar" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBlock x:Name="SpaceSavedText" FontSize="28" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="16" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Módulos activos" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBlock x:Name="ModulesText" FontSize="28" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</Page>
